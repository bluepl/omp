<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="omp.rifeinblu.com.ompmultilevellistviewnavigation" >

    <!-- Permission - Internet Connect -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Write External Storage Permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Record Audio Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AlbumsActivity"
            android:label="@string/title_activity_albums" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TrackListActivity"
            android:label="@string/title_activity_track_list" />
        <activity
            android:name=".SingleTrackActivity"
            android:label="@string/title_activity_single_track" />
        <!--
        <activity
            android:name=".AlertDialogManager"
            android:label="@string/title_activity_alert_dialog_manager" >
        </activity>
        <activity
            android:name=".ConnectionDetector"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_connection_detector"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".JSONParser"
            android:label="@string/title_activity_jsonparser" >
        </activity>
        -->
        <activity
            android:name=".MediaPlayerActivity"
            android:label="@string/title_activity_media_player" >
        </activity>
        <activity
            android:name=".UploadToServer"
            android:label="@string/title_activity_upload_to_server" >
        </activity>
        <activity
            android:name=".AudioCaptureActivity"
            android:label="@string/title_activity_audio_capture" >
        </activity>
        <activity
            android:name=".InsertActivity"
            android:label="@string/title_activity_insert" >
        </activity>
    </application>

</manifest>
